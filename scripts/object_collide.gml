// stop them from being on top of each other
// argument[0] should be the one pushing the other

// argument[0] the player you want to remain in location
// argument[1] the player you want to be pushed
// argument[2] the direction you want arg[1] to be pushed in
/*
switch (argument[2]){
    case 0:
        argument[1].x = argument[0].x + (argument[1].bbox_right - argument[1].bbox_left) + 40;
        break;
    case 90:
        argument[1].y = argument[0].y - (argument[1].bbox_bottom - argument[1].bbox_top) - 40;
        break;
    case 180:
        argument[1].x = argument[0].x - (argument[1].bbox_right - argument[1].bbox_left) - 40;
        break;
    case 270:
        argument[1].y = argument[0].y + (argument[1].bbox_bottom - argument[1].bbox_top) + 40;
        break;
}
*/
